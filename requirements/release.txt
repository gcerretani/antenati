# Pinned tooling used to build release artifacts (wheel/sdist, PyInstaller
# executables, CycloneDX SBOMs). Install with:
#   pip install -r requirements/release.txt
build==1.6.1
pyinstaller==6.22.3
cyclonedx-bom==7.4.0
